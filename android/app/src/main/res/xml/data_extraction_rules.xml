<?xml version="1.0" encoding="utf-8"?>
<!--
  Android 12+ (API 31+) backup and transfer rules. Must mirror
  backup_rules.xml, which covers API ≤ 30; see the reasoning there.

  Both destinations are excluded, and device-to-device transfer is the one that
  matters most here: it is the common "I got a new phone" path, and it copies
  more than cloud backup does.
-->
<data-extraction-rules>
  <cloud-backup>
    <exclude domain="file" path="datastore/" />
  </cloud-backup>
  <device-transfer>
    <exclude domain="file" path="datastore/" />
  </device-transfer>
</data-extraction-rules>
